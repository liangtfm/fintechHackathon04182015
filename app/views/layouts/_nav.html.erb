<nav>
  <a href="/" class='logo'>Affinity</a>

  <div class="actions">
  <% if current_user %>
    <a href='/profile' class='profile'>
      Signed in as <%= current_user.name %>
    </a> &nbsp; &nbsp;
    <%= link_to 'Log out', logout_path, {class: 'logout'} %>
  <% else %>
    <%= link_to 'Log in with Twitter', '/auth/twitter', {class: 'twitter'} %>
  <% end %>
  </div>
</nav>
